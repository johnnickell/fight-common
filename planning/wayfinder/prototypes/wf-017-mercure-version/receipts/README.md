# WF-017 Mercure version receipt

Generated by `../run.sh` against the official stable `v0.24.2` and testing-only `v1.0.0-alpha.3` hub
containers. Each receipt compares one exact private topic using the 0.x JWT/query shape and the 1.0 OAuth 2
access-token/query shape with compatibility mode disabled.

The receipt intentionally records no token, signing key, container name, or ephemeral local port.
