# Receipts

`run.php` rewrites one JSON receipt per framework. Each receipt records the selected transport, the exact
private destination, the public-envelope allowlist, and the post-commit invocation probe.
