# Handler-composition receipts

These JSON files are generated by `../run.php`. Each receipt records the pinned native container version,
composition style, resolved command/query/event map, dispatch results, and the three rejected boot scenarios.
