# Receipts

Generated by `../run.php`. Each receipt records whether the real framework adapter implements the narrower
transaction port, whether it also implements the legacy port, and whether it exposes `commit()`.
