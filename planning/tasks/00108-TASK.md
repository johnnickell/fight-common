---
id: TASK-00108
ticket: TICKET-00025
kind: feature
title: Orchestrate request-scoped MCP progress and cooperative cancellation
status: ready-for-agent
order: 5
blocked_by: TASK-00105,TASK-00107
pr:
---

# Orchestrate request-scoped MCP progress and cooperative cancellation

## Outcome

Deliver the reusable Application-layer execution behavior behind progressive MCP Tool calls. A valid protocol
progress token is associated with a live request-scoped `McpProgressReporter`; an absent token selects the no-op
reporter. The orchestrator preserves ordered progress before the final semantic response, observes advisory
cancellation set by the outer transport, and prevents duplicate or late output without claiming forced interruption
or rollback.

## Scope

- In scope:
  - Protocol-semantic progress-token shape validation for `tools/call`, accepting only the protocol-defined string
    or integer forms and associating the unchanged token with that request's reporter notifications.
  - A live implementation of TASK-00106's already accepted minimal `McpProgressReporter` contract and orchestration
    of exactly one TASK-00107 Tool invocation through it.
  - Strictly increasing finite numeric progress, optional numeric total and human-readable status, deterministic
    rejection of invalid or non-increasing reports before delivery, and exact token preservation.
  - Request-related progress notification representation, ordering before the final TASK-00104 semantic responder
    message, once-only completion, and suppression of reports after completion.
  - A request-scoped cancellation state set by an outer delivery adapter when its client connection closes;
    `isCancelled()` gives the Tool an advisory safe-point signal and all later client output is suppressed.
  - Package-owned query-backed and mutation-backed fixtures proving equivalent semantic Tool output with live and
    no-op reporters, expected and unexpected failure after progress, cancellation, and work already dispatched.
  - Additive compatibility-manifest classification and consumer-facing documentation for public additions.
- Out of scope:
  - HTTP response-mode selection, SSE serialization, response headers, flushing, disconnect detection, framework
    responses, server emission, and buffering control, which belong to [TASK-00109](00109-TASK.md).
  - Consumer routes, authentication, principals, permission policy, worker/runtime policy, gateway configuration,
    or deployment-specific timeouts.
  - Forced thread/process interruption, transaction rollback, undoing a dispatched command, durable Tasks,
    subscriptions, partial Tool-result streaming, legacy HTTP+SSE/session behavior, or a Streamable HTTP
    `notifications/cancelled` message for request-stream closure.
  - Generator-backed deferred Tool execution or transport callbacks, partial results, or framework response objects
    in `McpToolOutput`.

## Use cases and contracts

| Use case | Actor and trigger | Commands | Queries | Events | Expected side effects |
|---|---|---|---|---|---|
| Invoke without progress | Semantic `tools/call` execution has no progress token | At most the one consumer Command selected by TASK-00107 | At most the one consumer Query selected by TASK-00107 | N/A | Inject the no-op reporter, deliver no progress notification, invoke the Tool once, and preserve the ordinary final semantic response. |
| Invoke with progress | Semantic `tools/call` execution has a valid string or integer progress token | At most the one consumer Command selected by TASK-00107 | At most the one consumer Query selected by TASK-00107 | N/A - MCP protocol notifications are not Fight Events | Inject one live reporter, deliver request-related `notifications/progress` representations in strict order, then deliver the final semantic response once. |
| Report Tool status | The selected Tool reports progress while its request remains open | N/A beyond the Tool's owned use case | N/A beyond the Tool's owned use case | N/A - MCP protocol notifications are not Fight Events | Preserve the token and increasing numeric progress; optional total/status may be included, but final or partial Tool content is never carried. |
| Fail after progress | The selected Tool or final response preparation fails after progress while delivery remains open | No additional Command | No diagnostic Query | N/A | For an expected public-safe Tool failure, deliver one complete `isError` Tool result; for an unclassified `Throwable` or contract failure, log once and deliver one generic final JSON-RPC error; then complete without reinterpreting progress as success. |
| Close the request stream | An outer delivery adapter marks this request cancelled after observing client closure | No new Command | No new Query | N/A | Mark only this reporter cancelled, suppress every later progress/final representation, and let the Tool stop only at a safe cooperative checkpoint. |

## Validation and permissions

An absent progress token is valid and selects the no-op reporter. A present token must be a string or integer; an
array, object, boolean, null, floating-point value, or malformed metadata placement fails protocol validation before
Tool invocation. The client owns uniqueness across active requests; Common preserves and scopes the supplied token
without inventing a global session or accepting a token from another request.

The live reporter accepts only finite numeric progress that strictly increases for that request. Optional total and
status values must satisfy the already accepted public contract and cannot smuggle partial/final content. A
duplicate, decreasing, non-finite, or otherwise invalid report is rejected before delivery as a Tool contract
failure and contained by the existing safe central failure boundary. After final completion, later reports are
rejected without delivery. After cancellation, later progress and final responses are suppressed because the outer
transport has reported that no more client output is possible.

Failure behavior is state-specific. Selected-Tool input validation completes before Tool invocation and therefore
before any progress; its failure remains TASK-00107's complete `isError` Tool result with no progress delivered.
After progress and while delivery is open, an expected public-safe business failure becomes one final complete
`isError` Tool result. A late output-schema conformance failure, reporter-contract violation, response-preparation
failure, or unclassified `Throwable` is diagnosed once and becomes one sanitized final JSON-RPC error. After
completion, no duplicate final is delivered. After cancellation, no client-visible failure or final is delivered
and any diagnostic remains server-side.

Common makes no authentication or authorization decision. TASK-00105's consumer-supplied Origin and invocation
guards and TASK-00107's availability-before-invocation behavior remain authoritative. Cancellation observation is
advisory application input: a Tool may stop at a business-safe checkpoint, but Common neither rolls back work nor
promises that a command already dispatched can be undone.

## Acceptance criteria

- [ ] Semantic `tools/call` execution without a progress token receives exactly the no-op reporter, delivers no
  progress notification, invokes the selected Tool once, and preserves the ordinary final semantic response.
- [ ] A valid string or integer progress token selects exactly one live request-scoped reporter, is preserved
  without coercion in every related notification representation, and never leaks into Tool input or CQRS payload.
- [ ] Valid progress reports are represented as request-related `notifications/progress` messages in strictly
  increasing order before the final response; completion delivers the final semantic responder message once.
- [ ] Selected-Tool input validation finishes before invocation and progress; rejection delivers TASK-00107's
  complete `isError` Tool result with no progress. An expected public-safe business failure after earlier progress
  also delivers one complete `isError` Tool result.
- [ ] A late output-schema conformance failure, reporter-contract violation, response-preparation failure, or
  unclassified `Throwable` after progress delivers one centrally classified, public-safe final JSON-RPC error and
  is logged at most once while delivery remains open.
- [ ] The same failure after cancellation delivers nothing; a report after completion cannot create another
  progress or final message, and neither path converts earlier progress into partial success.
- [ ] Duplicate, decreasing, non-finite, malformed, late, or content-bearing progress cannot be delivered.
- [ ] Progress carries only numeric work, optional total, and human-readable status. Structured output, text
  output, application Views, and every other partial/final result remain in `McpToolOutput` and the final response.
- [ ] When an outer adapter marks the request cancelled, `isCancelled()` becomes true, no later progress or final
  response is delivered, and fixtures prove both early cooperative stop and already-dispatched work without a
  rollback claim.
- [ ] Cancellation is request-scoped and race-safe: closure and completion cannot produce duplicate finals,
  cross-request cancellation, post-completion progress, or a Streamable HTTP `notifications/cancelled` message.
- [ ] TASK-00106's stable reporter interface is not broadened speculatively. Any implementation convenience remains
  private until TASK-00109 adapter proof establishes that a public change is unavoidable and additive.
- [ ] Every public addition is classified in `compatibility/manifest.json`, its promised operations are explicit,
  and `CONTEXT.md` plus affected consumer documentation accurately describe the implemented boundary.

## Verification and evidence

Use focused unit/contract tests for token validation, live/no-op reporter selection, exact token preservation,
strictly increasing numeric progress, optional total/status, invalid-report rejection, progress/output separation,
ordering, pre-invocation selected-Tool validation with no progress and a complete `isError` result, expected
business failure after progress with one final complete `isError` result, late output/reporter/response or
unclassified failure after progress with one sanitized JSON-RPC error and one diagnostic, once-only completion,
post-completion rejection, outer cancellation, post-cancellation suppression, cross-request isolation, and
cancellation races. Exercise package-owned query-backed and
mutation-backed Tools through TASK-00104's dispatcher and TASK-00107's selected-Tool invoker with controlled
semantic delivery; do not claim that controlled delivery proves a real HTTP stream.

This is a library/API change with no useful visual UI. Before evidence is TASK-00107's no-op reporter and direct
semantic `tools/call` transcript. After evidence is a controlled multi-message transcript showing at least two
progress notifications before the same final result, plus expected/unexpected failure, cancellation, and no-token
transcripts with delivery, bus, and diagnostic spies. Save artifacts and the complete `./bin/build` log/exit
receipt under the TASK's ignored run directory. Run the canonical `./bin/build` before publication and surface
warnings, protocol ambiguity, cancellation races, or incomplete compatibility evidence.

## Coordination

TASK-00105 supplies the guarded HTTP request foundation; TASK-00107 supplies Tool selection/invocation, semantic
output, failure classification, and the stable reporter type. Application owns reporter semantics and Tool
orchestration. TASK-00109 owns HTTP extraction and response-mode selection, SSE framing, emitter lifecycle,
flushing, disconnect observation, and framework response translation.

Use dependency-ordered SUBTASKs for protocol values and reporter behavior, semantic execution and failure-state
handling, package fixtures and race tests, compatibility classification, `CONTEXT.md`, and documentation inside
this one PR. If ordered progress and cooperative cancellation cannot be expressed through TASK-00106's accepted
reporter signature and synchronous Tool invocation without transport callbacks in `McpToolOutput`, stop and return
the exact contradiction for decision rather than broadening the interface or weakening the contract.

## Completion notes

This planning record is approved for publication in the existing planning PR. No implementation, implementation
review, implementation publication, merge, release, or deployment evidence exists. Refresh generated planning
views and record actual verification and compatibility classifications when the TASK is implemented.
